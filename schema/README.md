# schema/  — SUPERSEDED

The SQL in `_superseded_40table/` implements the earlier 40-table design.
It is retained for reference only and is **not** the current schema.

The live schema is `db/migrations/`, generated from
`WF-001_Schema_Blueprint.docx` (37 tables, 267 columns, 54 foreign keys).
